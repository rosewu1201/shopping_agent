{
  "name": "barbie-collector-hub",
  "version": "1.0.0",
  "description": "Barbie Collector Hub - Live price comparison across platforms",
  "scripts": {
    "start": "cd server && node server.js",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
